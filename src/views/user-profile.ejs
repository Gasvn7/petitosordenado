<%- include('partials/head', {title: "Perfil de " + user.first_name }) %>
    <%- include('partials/header') %>

        <body>
            <p>Hola <%= user.first_name + ' ' + user.last_name %>
            </p>
            <img src="/img/usersImages/<%= user.image %>" alt="imagen de perfil"><br>
            <span>
                <%= user.email %>
            </span><br>
            <form action="/users/logout" method="POST">
                <input type="submit" value="Logout">
            </form>
        </body>

        </html>