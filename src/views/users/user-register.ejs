<head>
    <%- include('../partials/head', {title: "Crea tu cuenta - Petitos Shop" }) %>
</head>

<%- include('../partials/header') %>
    <!----Formulario-Register-->
    <main class="main-register-login">
        <form action="/users" method="POST" enctype="multipart/form-data" class="form-login">
            <div class="container">
                <h1>Registrarme</h1>
                <p>Por favor completa el formulario para registrarte.</p>
                <hr>

                <label for="first_name"><b>¿Cuál es tu nombre?</b></label>
                <input class="input" type="text" name="first_name" id="first_name"
                    class="form-control <%= locals.errors && errors.first_name ? 'is-invalid' : null %> ">
                <div class="text-danger-first_name">
                    <% if (locals.errors && errors.first_name) { %>
                        <%= errors.first_name.msg %>
                            <% } %>
                </div>
                <label for="last_name"><b>¿Cuál es tu apellido?</b></label>
                <input class="input" type="text" name="last_name" id="last_name"
                    class="form-control <%= locals.errors && errors.last_name ? 'is-invalid' : null %> ">
                <div class="text-danger-last_name">
                    <% if (locals.errors && errors.last_name) { %>
                        <%= errors.last_name.msg %>
                            <% } %>
                </div>
                <label for="email"><b>Ingresa tu email</b></label>
                <input class="input" type="email" name="email" id="email"
                    class="form-control <%= locals.errors && errors.email ? 'is-invalid' : null %> ">
                <div class="text-danger-email">
                    <% if (locals.errors && errors.email) { %>
                        <%= errors.email.msg %>
                            <% } %>
                </div>
                <label for="password"><b>Ingresa una contraseña</b></label>
                <input class="input" type="password" name="password" id="password"
                    class="form-control <%= locals.errors && errors.password ? 'is-invalid' : null %> ">
                <div class="text-danger-password">
                    <% if (locals.errors && errors.password) { %>
                        <%= errors.password.msg %>
                            <% } %>
                </div>
                <label for="image"><b>Foto de perfil</b></label>
                <input type="file" name="image" id="image">
                <p>Al crearte una cuenta aceptas los <a href="#" style="color:dodgerblue">Términos y
                        condiciones.</a>
                </p>
                <div class="text-danger-text">
                    <% if (locals.errors && errors.text) { %>
                        <%= errors.text.msg %>
                            <% } %>
                </div>
                <div class="clearfix">
                    <button class="login-button" type="submit">
                        Crear cuenta
                    </button>
                    <a href="/users/login" class="register-button">
                        Ya tengo cuenta
                    </a>
                </div>
            </div>
        </form>
    </main>
    <!-- <script src="/js/validationsRegister.js"></script> -->
    </body>
    <%- include('../partials/footer') %>

        </html>